# block-secrets allowlist: path substrings that should NOT be blocked even though
# they match a blocked pattern (.env, credentials, secrets, api-key, .pem, .key).
# One entry per line. Use ONLY for source files that merely mention the word and
# hold no real secret value (e.g. a file named client-secrets.ts that defines
# functions). NEVER list a real .env or credential store here.
